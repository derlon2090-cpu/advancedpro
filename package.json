{
  "name": "advancedpro-root",
  "private": true,
  "description": "Root package.json to proxy Render commands to backend",
  "scripts": {
    "build": "npm --prefix backend install",
    "render:build": "npm run build",
    "start": "npm --prefix backend start"
  }
}
